{
  "name": "nats-apollo-server",
  "version": "1.0.0",
  "description": "Apollo Server with NATS pub/sub and GraphQL subscriptions.",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "apollo-server-express": "^3.10.0",
    "graphql": "^15.5.0",
    "graphql-ws": "^5.7.0",
    "graphql-tools": "^8.3.0",
    "express": "^4.17.1",
    "nats": "^1.0.0",
    "ws": "^8.2.3",
    "graphql-subscriptions": "^1.2.0",
    "https": "^1.0.0",  // Built-in module, not required as a dependency
    "fs": "^0.0.1-security" // Built-in module, not required as a dependency
  },
  "devDependencies": {
    "nodemon": "^2.0.15" // Optional for development to auto-restart the server
  },
  "keywords": [
    "apollo",
    "nats",
    "graphql",
    "subscriptions"
  ],
}
